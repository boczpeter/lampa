{
  "name": "lampa",
  "version": "1.5.1",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "package": "vite package",
    "preview": "vite preview",
    "reload": "cp -v apache/lampa.conf /etc/apache2/sites-available/ && sudo service apache2 reload",
    "live": "rsync -avi --no-times --checksum build/ bp:/var/www/lampa/",
    "live-cleanup": "rsync -avi --no-times --checksum --delete build/ bp:/var/www/lampa/",
    "compare": "rsync -navi --no-times --checksum build/ bp:/var/www/lampa/"
  },
  "devDependencies": {
    "@iconify/svelte": "^3.0.0",
    "@sveltejs/adapter-static": "^1.0.0-next.42",
    "@sveltejs/kit": "^1.0.0-next.505",
    "mdsvex": "^0.10.6",
    "svelte": "3.51.0",
    "vite": "^3.1.4"
  },
  "type": "module",
  "dependencies": {
    "leaflet": "^1.9.1"
  }
}
